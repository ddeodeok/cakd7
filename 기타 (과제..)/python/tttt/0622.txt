1. pandas as pd 
pd.Series()

2. enumerate(a)

3. sys.argv

4. lambda

5. map

6. filter

0622

1. pickle 활용해서 파일 (자료형) 저장하고 그대로 불러오기

2. os 
	1. PY 라는 디렉토리를 만듭시다
	2. PY 디렉토리를 워킹디렉토리로 지정합니다
	3. 워킹	디렉토리를 확인합시다.
	4. '1,2,3,4,5,...10,' 가 들어있는 number.txt를 저장합시다.
	5. number.txt 를 지웁시다. # <- os.remove("파일이름")
	6. 워킹디렉토리를 C:/Users/admin 으로 합시다
	7. PY디렉토리를 지웁니다.
3. glob 화용해서 폴더안의 텍스트 또는 모든 파일 보여주기

4. time 현재 년도부터 초까지 보여주기, 현재시간,

5. random  
	1. 0부터 1까지 난수
	2. a,b 사이의 임이의 수
	3. 임의 리스트 값중 랜덤선택
	4. 리스트 순서 섞기
	5. a,b 사이 임이의 수 (소수점 포함)
	6. 같은 환경의 랜덤결과가 같게 나오게하기
6. threading
	1. 쓰레드를 활용하여 같은일 5개 반복하는걸 짧은시간에 하게만들기
	2. 조인 함수 사용하여 마지막 문자가 끝에 나오게하기

7. 리스트를 딕셔너리로 만들기




루프스, 임파선염